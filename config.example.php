<?php
// ============================================================
// StarIDC 配置示例文件（config.example.php）
// ------------------------------------------------------------
// 部署步骤：
//   1. 复制本文件为 config.php（命令：cp config.example.php config.php）
//   2. 修改下方 $dbconfig 为你自己的数据库信息（必填）
//   3. 如需自定义主站地址 / MNBT 根目录 / 调试模式，取消对应行的注释并修改
//   4. 在浏览器访问你的域名，程序会自动跳转到 /install/ 完成建库与管理员创建
//
// 重要说明：
//   - 本文件【不会被程序读取】，程序只读取 config.php；
//   - 其余运行时配置（MNBT / 支付 / 邮件 API 密钥等）存于数据库 config 表，
//     安装后在后台「系统配置」中管理，无需写在本文件。
// ============================================================

// 主站数据库配置（必填项，部署时改成你自己的）
$dbconfig = array(
    'host'   => 'localhost',         // 数据库主机，通常为 localhost；远程库填 IP 或域名
    'port'   => 3306,                // 数据库端口，MySQL 默认 3306
    'user'   => 'your_db_user',      // 数据库用户名
    'pwd'    => 'your_db_password',  // 数据库密码
    'dbname' => 'your_db_name',      // 数据库名（需提前创建，或授权自动创建）
);

// ===================== 可选覆盖（取消注释即可生效）=====================
// 主站访问地址（用于生成绝对链接、回调地址等；留空则自动探测当前域名）
// define('SITE_URL', 'https://your-site.example.com');
// 调试模式（true 时页面显示 PHP 错误详情；生产环境务必保持 false）
// define('DEBUG', false);
?>
